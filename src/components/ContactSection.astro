---
    import SocialMedia from './SocialMedia.astro'
---


<section id="contact" class="min-h-[100dvh] flex flex-col items-center justify-center py-12">
    <div class="mx-auto w-[80%] md:w-[80%] lg:w-full">
        <div class="parent">
            <div class="div1 card rounded-xl">
                <h2>¿Tienes alguna idea o proyecto en mente?</h2>
                <SocialMedia/>
            </div>
            <div class="div2 card rounded-xl">
                <h3>Nombre</h3>
                <div class="grid py-3 h-full">
                    <input type="text" placeholder="Nombre" class="w-full p-2 rounded bg-white/5 border border-white/10">
                    <input type="email" placeholder="Email" class="w-full p-2 mt-2 rounded bg-white/5 border border-white/10">
                    <input type="tel" placeholder="Teléfono" class="w-full p-2 mt-2 rounded bg-white/5 border border-white/10">
                </div>
            </div>
            <div class="div3 card rounded-xl">
                <h3>Mensaje</h3>
                <textarea placeholder="Escribe tu mensaje aquí..." class="w-full h-full p-2 mt-2 rounded bg-white/5 border border-white/10"></textarea>
            </div>
            <div class="div4 card rounded-xl flex justify-center">
                <button class="button w-full">Enviar</button>
            </div>
        </div>  
    </div>
</section>

<style>

    .parent {
        display: grid;
        grid-template-columns: repeat(5, 1fr);
        grid-template-rows: repeat(5, 1fr);
        grid-column-gap: 12px;
        grid-row-gap: 12px;
        padding: 0 1rem;
    }

    @media (max-width: 1024px) {
        .parent {
            grid-template-columns: repeat(4, 1fr);
            grid-template-rows: auto;
        }

        .div1 { 
            grid-area: 1 / 1 / 2 / 5 !important;
        }
        
        .div2 { 
            grid-area: 2 / 1 / 3 / 5 !important;
        }
        
        .div3 { 
            grid-area: 3 / 1 / 4 / 5 !important;
        }
        
        .div4 { 
            grid-area: 4 / 1 / 5 / 5 !important;
        }
    }

    @media (max-width: 640px) {
        .parent {
            grid-template-columns: 1fr;
            gap: 12px;
            padding: 0 0.5rem;
        }

        .div1, .div2, .div3, .div4 { 
            grid-area: auto !important;
        }
    }

    .card {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
        padding: 1.5rem;
        transition: all 0.2s ease-in-out;
        backdrop-filter: blur(10px);
    }

    @media (max-width: 640px) {
        .card {
            padding: 1rem;
        }
    }

    .card:hover {
        transform: scale(1.01);
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
        backdrop-filter: blur(3px);
    }

    .div1 { 
        grid-area: 1 / 2 / 3 / 5;
        display: flex;
        flex-direction: column;
        justify-content: center;
        text-align: center;
    }
    
    .div2 { 
        grid-area: 3 / 2 / 5 / 3;
        display: flex;
        flex-direction: column;
    }
    
    .div3 { 
        grid-area: 3 / 3 / 5 / 5;
        display: flex;
        flex-direction: column;
    }
    
    .div4 { 
        grid-area: 5 / 2 / 6 / 5;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    h2 {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
    }

    @media (max-width: 640px) {
        h2 {
            font-size: 1.25rem;
        }
    }

    h3 {
        font-size: 1.1rem;
        margin-bottom: 0.75rem;
        color: rgba(255, 255, 255, 0.9);
    }

    @media (max-width: 640px) {
        h3 {
            font-size: 1rem;
        }
    }

    input, textarea {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: white;
        border-radius: 0.5rem;
        padding: 0.75rem;
        margin-bottom: 0.5rem;
        outline: none;
        transition: all 0.2s ease;
    }

    input:focus, textarea:focus {
        border-color: #07f9a2;
        box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.1);
    }



.button {
  /* in scss with just one variable i can change opacity with rgba(variable, opacity) but in css it's not possible so i have used three seperate variables */
  /* with hue-rotate color can be changed */
  --main-color: #07f9a2;
  --main-bg-color: rgba(46, 213, 116, 0.36);
  --pattern-color: rgba(46, 213, 116, 0.073);

  /* change this rotation value */
  filter: hue-rotate(0deg);

  cursor: pointer;
  text-transform: uppercase;
  letter-spacing: 0.5rem;
  background: radial-gradient(
      circle,
      var(--main-bg-color) 0%,
      rgba(0, 0, 0, 0) 95%
    ),
    linear-gradient(var(--pattern-color) 1px, transparent 1px),
    linear-gradient(to right, var(--pattern-color) 1px, transparent 1px);
  background-size:
    cover,
    15px 15px,
    15px 15px;
  background-position:
    center center,
    center center,
    center center;
  border-image: radial-gradient(
      circle,
      var(--main-color) 0%,
      rgba(0, 0, 0, 0) 100%
    )
    1;
  border-width: 1px 0 1px 0;
  color: var(--main-color);
  padding: 1rem 3rem;
  font-weight: 700;
  font-size: 1.5rem;
  transition: background-size 0.2s ease-in-out;
}

@media (max-width: 768px) {
  .button {
    padding: 0.75rem 2rem;
    font-size: 1.25rem;
    letter-spacing: 0.3rem;
  }
}

@media (max-width: 640px) {
  .button {
    padding: 0.75rem 1.5rem;
    font-size: 1rem;
    letter-spacing: 0.2rem;
  }
}

.button:hover {
  background-size:
    cover,
    10px 10px,
    10px 10px;
}
.button:active {
  filter: hue-rotate(250deg);
}


</style>